<!-- 이 파일은 docs/lessons.js 에서 자동 생성됩니다. 직접 고치지 말고 `node tools/gen-tutorial.js` 를 쓰세요. -->

# Venos 배우기

*[English](TUTORIAL.md) | 한국어*

설치 없이 브라우저에서 한 단계씩 따라 하는 Venos 입문서입니다. 각 레슨의 **플레이그라운드에서 열기** 링크를 누르면 그 레슨의 코드가 바로 실행 가능한 상태로 열립니다.

변수와 함수 이름은 한국어로 지어도 됩니다. 반면 `if` / `for` / `while` / `func` / `class` 같은 키워드는 영어라서, 여기서 배운 구조가 나중에 파이썬이나 C 로 그대로 이어집니다.

## 목차

1. [화면에 출력하기](#1-화면에-출력하기)
2. [변수 — 값에 이름 붙이기](#2-변수-값에-이름-붙이기)
3. [입력받기와 문자열 보간](#3-입력받기와-문자열-보간)
4. [조건 — if / else](#4-조건-if-else)
5. [반복 — for](#5-반복-for)
6. [조건이 참인 동안 — while](#6-조건이-참인-동안-while)
7. [리스트 — 값을 여러 개](#7-리스트-값을-여러-개)
8. [딕셔너리 — 이름표가 붙은 값](#8-딕셔너리-이름표가-붙은-값)
9. [함수 — 이름 붙인 동작](#9-함수-이름-붙인-동작)
10. [클래스 — 나만의 자료형](#10-클래스-나만의-자료형)
11. [에러 다루기 — try / catch](#11-에러-다루기-try-catch)
12. [미니 프로젝트 — 숫자 맞히기](#12-미니-프로젝트-숫자-맞히기)

---

## 1. 화면에 출력하기

`print` 는 값을 화면에 보여줍니다. 쉼표로 여러 개를 한 줄에 쓸 수 있어요. `#` 뒤는 주석이라 실행되지 않습니다.

▶ Run 을 눌러보고, 따옴표 안의 글자를 바꿔 다시 실행해 보세요.

```
# 첫 번째 Venos 프로그램
print "안녕하세요!"
print "반가워요 :)"

# 쉼표로 여러 값을 한 줄에
print "1 + 2 =", 1 + 2
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=print)**

---

## 2. 변수 — 값에 이름 붙이기

`let` 으로 변수를 만듭니다. **변수 이름은 한국어로 지어도 됩니다.**

한 번 만든 뒤에는 `let` 없이 값을 바꿀 수 있고, `+=` 로 더할 수도 있어요.

▶ `나이` 를 바꿔서 실행해 보세요.

```
let 이름 = "미르"
let 나이 = 15

print 이름
print 나이

나이 += 1          # 한 살 더
print "내년 나이:", 나이
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=variables)**

---

## 3. 입력받기와 문자열 보간

`input` 은 사용자에게 값을 물어봅니다. 플레이그라운드에서는 작은 창이 뜹니다.

문자열 안에 `{ }` 를 쓰면 그 안의 값이 그대로 끼워집니다 — 이걸 **문자열 보간**이라고 해요. `+` 로 이어붙이는 것보다 읽기 쉽습니다.

▶ 실행하면 이름을 물어봅니다. 아무 이름이나 넣어 보세요.

```
let 이름 = input "이름이 뭐예요? "
let 나이 = 15

print "안녕하세요, {이름}님!"
print "{이름}님은 내년에 {나이 + 1}살이 되네요."
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=input)**

---

## 4. 조건 — if / else

`if` 는 조건이 참일 때만 `{ }` 안을 실행합니다. 아니면 `else if`, 그것도 아니면 `else` 로 넘어가요.

`then` 은 써도 되고 안 써도 됩니다 — 읽기 좋으라고 있는 단어예요.

▶ `점수` 를 바꿔가며 실행해 등급이 어떻게 달라지는지 보세요.

```
let 점수 = 85

if 점수 >= 90 then {
    print "A 등급"
} else if 점수 >= 80 {
    print "B 등급"
} else {
    print "더 힘내요!"
}

print "점수는 {점수}점입니다."
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=if)**

---

## 5. 반복 — for

`for i = 1 to 5` 는 1부터 5까지 **양 끝을 포함해서** 반복합니다.

`step` 으로 건너뛸 수도 있어요. 거꾸로 세려면 `step -1` 처럼 음수를 씁니다.

`"*" * i` 는 별을 i 개 이어붙인 문자열이라 반복문으로 그림을 그릴 수 있어요.

▶ 숫자를 바꿔 구구단을 다른 단으로 바꿔 보세요.

```
# 3단 구구단
for i = 1 to 9 {
    print "3 x {i} = {3 * i}"
}

print ""
# 거꾸로 세기
for i = 5 to 1 step -1 {
    print i
}
print "발사!"

print ""
# 별 쌓기
for i = 1 to 5 { print "*" * i }
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=for)**

---

## 6. 조건이 참인 동안 — while

`while` 은 조건이 참인 **동안** 계속 반복합니다. 몇 번 반복할지 미리 모를 때 씁니다.

`break` 로 즉시 빠져나올 수 있어요. 조건을 언젠가 거짓으로 만들지 않으면 영원히 돌게 되니 주의하세요.

▶ `남은돈` 을 바꿔 몇 번 살 수 있는지 보세요.

```
let 남은돈 = 1000
let 가격 = 300
let 개수 = 0

while 남은돈 >= 가격 do {
    남은돈 -= 가격
    개수 += 1
}

print "{개수}개 샀고, {남은돈}원 남았어요."
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=while)**

---

## 7. 리스트 — 값을 여러 개

리스트는 값을 순서대로 담습니다. **번호는 1부터 시작해요** (0이 아닙니다!).

`push` 로 뒤에 추가하고, `len` 으로 개수를 세고, `for ... in` 으로 하나씩 꺼내 봅니다.

▶ 과일을 더 추가해 보세요.

```
let 과일 = ["사과", "바나나", "포도"]

print "첫 번째:", 과일[1]      # 1번부터!
print "개수:", len(과일)

push(과일, "딸기")

for 하나 in 과일 {
    print "-", 하나
}
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=lists)**

---

## 8. 딕셔너리 — 이름표가 붙은 값

딕셔너리는 **이름표(키)** 로 값을 찾습니다. 키는 항상 문자열이에요.

없는 키를 읽으면 에러가 나니, `has` 로 먼저 확인하는 습관을 들이세요.

▶ 과목과 점수를 더 넣어 보세요.

```
let 성적 = {"수학": 90, "영어": 85}

성적["과학"] = 95        # 새 키는 넣으면 생김
성적["수학"] += 5

for 과목 in 성적 {
    print "{과목} → {성적[과목]}점"
}

if has(성적, "체육") {
    print "체육:", 성적["체육"]
} else {
    print "체육 점수는 아직 없어요."
}
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=dicts)**

---

## 9. 함수 — 이름 붙인 동작

`func` 로 동작에 이름을 붙여 두면 몇 번이든 다시 쓸 수 있습니다. `return` 으로 결과를 돌려줘요.

함수는 **자기 자신을 부를 수도** 있습니다(재귀). 아래 `팩토리얼` 이 그 예예요.

▶ `인사("이름")` 을 한 줄 더 추가해 보세요.

```
func 인사(이름) {
    print "안녕하세요, {이름}님!"
}

func 팩토리얼(n) {
    if n <= 1 then { return 1 }
    return n * 팩토리얼(n - 1)
}

인사("미르")
인사("하늘")
print "5! =", 팩토리얼(5)
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=functions)**

---

## 10. 클래스 — 나만의 자료형

클래스는 **값과 동작을 한 덩어리로** 묶습니다. `init` 은 만들어질 때 자동으로 불리는 생성자예요.

`self` 는 "나 자신"을 가리킵니다. `self.이름` 처럼 자기 값을 읽고 쓰죠.

▶ 강아지를 한 마리 더 만들어 보세요.

```
class 강아지 {
    func init(이름) {
        self.이름 = 이름
        self.나이 = 0
    }
    func 짖기() { print "{self.이름}: 멍멍!" }
    func 생일() {
        self.나이 += 1
        print "{self.이름}(은)는 이제 {self.나이}살"
    }
}

let 뭉치 = 강아지("뭉치")
뭉치.짖기()
뭉치.생일()
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=classes)**

---

## 11. 에러 다루기 — try / catch

에러가 나면 프로그램이 멈춥니다. `try` 안에서 에러가 나면 프로그램을 멈추는 대신 `catch` 로 넘어가요.

에러 메시지는 `catch` 뒤에 적은 변수에 문자열로 담깁니다. `error(...)` 로 직접 에러를 낼 수도 있어요.

▶ 나누는 수를 0이 아닌 값으로 바꿔 실행해 보세요.

```
let 나누는수 = 0

try {
    print 10 / 나누는수
    print "이 줄은 실행되지 않아요"
} catch 오류 {
    print "문제가 생겼어요:", 오류
}

print "그래도 프로그램은 계속됩니다."
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=errors)**

---

## 12. 미니 프로젝트 — 숫자 맞히기

지금까지 배운 걸 전부 씁니다 — 변수, `while`, `if`, `input`, 함수, 문자열 보간.

`random(1, 50)` 은 1부터 50 사이 숫자를 아무거나 고릅니다.

▶ 범위를 넓히거나, 시도 횟수 제한을 넣거나, 힌트를 더 친절하게 만들어 보세요. 완성했으면 **Share** 버튼으로 친구에게 보내보세요!

```
let 정답 = random(1, 50)
let 시도 = 0

print "1부터 50 사이 숫자를 맞혀보세요!"

while true {
    let 답 = input "숫자: "
    시도 += 1

    if 답 == 정답 {
        print "정답! {시도}번 만에 맞혔어요 🎉"
        break
    } else if 답 < 정답 { print "더 큰 수예요 ↑" }
    else { print "더 작은 수예요 ↓" }
}
```

▶ **[플레이그라운드에서 열기](https://vpdrla.github.io/Venos/#lesson=project)**

---

## 다음은

- 전체 문법은 [언어 명세](VENOS_SPEC.md)에서 볼 수 있습니다.
- 더 큰 예제: [`examples/rpg.my`](examples/rpg.my) — 지금까지 배운 것만으로 만든 226줄짜리 텍스트 RPG입니다.
- 만든 프로그램은 플레이그라운드의 **🔗 Share** 버튼으로 링크를 만들어 친구나 선생님에게 보낼 수 있습니다.
- **다음 언어로 넘어갈 때**: 플레이그라운드의 **🐍 Python** 버튼(또는 `venos topython 파일.my`)을 누르면 지금 짠 프로그램이 파이썬으로 어떻게 생겼는지 그대로 보여줍니다. 변수·함수 이름도 그대로 남습니다.
