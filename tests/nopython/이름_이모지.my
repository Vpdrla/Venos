# Venos 의 이름 규칙은 0x80 이상을 전부 받아 준다 — 그래서 이 프로그램은 인터프리터와
# build 에서 멀쩡히 돈다. 그런데 파이썬은 이모지를 이름으로 못 써서, 그대로 옮기면
# **파싱조차 안 되는 .py** 가 나간다 (SyntaxError: invalid character).
# 틀린 답보다 나쁜 자리라 — 학생이 받은 파일은 한 줄도 안 돈다 — 여기서 거절한다.
let 🍎 = 3
print 🍎 + 1
