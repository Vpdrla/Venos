# Venos 는 어디에 있어야 하는가

*[English](STRATEGY.md) | 한국어*

이 문서는 하나만 답한다. **Venos 는 무엇을 위한 것이고, 무엇이 되기를 거부해야 하는가?**
다른 언어들이 자기 자리를 어떻게 찾았는지 — 또는 어떻게 잃었는지 조사해서 쓴 것이다.

## 1. 살아남은 언어는 전부 "자리" 하나를 갖고 있다

"여러 가지를 잘한다"가 아니다. **거기서는 이게 제일 편하다**는 자리 하나다.

| 언어 | 갖고 있는 자리 | 그 대가로 치르는 것 |
|---|---|---|
| Python | 데이터·AI·입문 | 느림, 패키지 설치 지옥 |
| JavaScript / TypeScript | 브라우저가 돌리는 유일한 언어 | 쌓인 문법 잡음, 생태계 과열 |
| C | 하드웨어와 직접 말하기 | 안전장치 없음 |
| C++ | 게임, 고성능 | 복잡도 폭발 |
| Java / C# | 기업, 안드로이드, Unity | 장황함 |
| Rust / Go | 안전한 시스템 / 단순한 서버 | 학습곡선 / 표현력 |
| SQL | 데이터에 질문하기 | 범용이 아님 |
| Scratch, 엔트리 | 어린이의 첫 코드 경험 | **텍스트로 안 이어짐** |

이런 자리가 없는 언어는 잘 만들었다는 이유만으로 살아남지 못한다. **어딘가에서는 당연한 선택**이
되어야 하고, 그러지 못하면 어디에서도 선택되지 않는다.

## 2. 배울 게 많은 실패들

**Pascal** 은 교육용으로 설계됐고 그 일을 아주 잘했다. 그러다 진짜 범용 언어가 되려고 했고,
Brian Kernighan 의 [*Why Pascal is Not My Favorite Programming
Language*](https://www.cs.princeton.edu/~bwk/btl.mirror/notfavorite.html) 이 왜 그럴 수 없는지를
조목조목 정리해 버렸다. 두 번째 싸움에서 지면서 첫 번째 자리까지 같이 잃었다. 지금 초보자는
Scratch 로 시작해 Python 으로 간다. Pascal 은 대부분의 교육과정에서 사라졌다.

**한국어 키워드 언어들**(약속, 와글, 새싹)은 키워드를 한국어로 바꾸고 설치를 요구했다.
키워드 번역은 막다른 길이다 — 구조가 하나도 안 넘어가서 **전환을 오히려 더 어렵게** 만든다.
설치를 요구하는 건 아무것도 설치할 수 없는 학교에서는 치명적이다.

둘에서 나오는 교훈은 같다. **교육용 언어는 자기가 임시라는 걸 잊는 순간 죽는다.**

## 3. 배울 게 많은 성공들

**[Portugol Studio](https://github.com/UNIVALI-LITE/Portugol-Studio)** (브라질 UNIVALI) 는
포르투갈어 의사코드 언어로 **9만 회 이상 다운로드**됐고 브라질 대학·공업고교에서 널리 쓰인다.
성공한 이유는 **스스로 다리라고 선언하고 나가는 시점을 명시했기 때문**이다 — 수업은 4주차쯤
Java 로 넘어간다. 종착지라고 주장한 적이 없다.

**[HAGGIS](https://en.wikipedia.org/wiki/Haggis_(programming_language))** 는 스코틀랜드 SQA 시험의
공식 의사코드다. 모든 학교·출제자·학생이 공유하는 단 하나의 표기이고, 프로그램을 짜는 게 아니라
시험문제의 코드를 **읽기** 위한 것이다. 재밌는 건 SQA 가 실행기를 안 만들어서 **제3자가 인터프리터를
따로 만들었다**는 점이다. **실행되지 않는 의사코드는 수요를 만든다.**

## 4. 한국에 비어 있는 자리

사실 세 가지가 맞물린다.

- 2022 개정 교육과정으로 정보 시수가 **2배**가 됐다 — 초등 17 → 34시간, 중학교 34 → 68시간.
  2025년 중1·고1부터 순차 적용된다
  ([출처](https://www.kyobit.com/news/articleView.html?idxno=2308)). 수요가 지금 늘고 있다.
- 교과서는 알고리즘을 **순서도와 의사코드**로 가르치는데, **그 의사코드는 실행이 안 된다.**
  한국에는 HAGGIS 같은 게 없다 — 공유되는 표기도, 그걸 돌려주는 실행기도.
- 블록에서 텍스트로 넘어가는 어려움은
  [연구로 정리돼 있다](https://www.sciencedirect.com/science/article/pii/S2590118425000280) —
  타이핑과 철자, 해독 불가능한 에러, 프로그래밍 모델 자체의 전환이 한꺼번에 닥친다.

그리고 Venos 는 이미 교과서가 쓰는 모양을 하고 있다: `if x >= 90 then`, `for i = 1 to 10`,
`while x > 0 do`. 그냥 넘길 우연이 아니다.

## 5. 자리

> **Venos 는 실행되는 의사코드이고, 블록 코딩에서 파이썬으로 건너가는 다리다.**

Portugol 모델을 따르되 Portugol 이 못 한 것을 한다. Portugol 학생은 Java 로 넘어갈 때 손으로 다시
쓴다. Venos 학생은 `venos topython` 을 치거나 브라우저에서 **🐍 Python** 을 누르면, 자기가 지은
변수·함수 이름이 그대로 살아 있는 파이썬을 받는다.

이 기능 하나는 동시에 **구조적인 약속**이다. 나가는 길을 직접 만들어 파는 언어는 슬그머니
Pascal 이 될 수 없다.

걸림돌 셋을 걷어내되, 알맹이는 하나도 걷어내지 않는다.

| 걸림돌 | Venos 가 하는 것 | 일부러 하지 않는 것 |
|---|---|---|
| 낯선 문법 | 교과서 의사코드처럼 읽힘 | 블록이 아니다 — 진짜 코드를 직접 친다 |
| 영어로만 되는 이름 | 식별자는 어떤 언어로든 (한글 등) | **키워드는 영어 유지** — 구조가 그대로 넘어가게 |
| 못 읽는 에러 | 한국어 에러 + 줄 번호 + 그 줄 표시 | 에러가 있다는 사실을 숨기지 않는다 |
| 아무것도 설치 못 함 | 브라우저에서 전부 실행 | 진짜 네이티브 바이너리도 같이 낸다 |

## 6. 그래서 무엇을 만들고 무엇을 안 만드는가

**만든다:** 교과서 알고리즘을 표현하고, 실행하고, 옮길 수 있게 하는 것 —
파이썬으로 나가는 길, 교과서 알고리즘 예제집([`examples/algorithms/`](examples/algorithms/)),
초보자용 에러 메시지 개선, 교실 배관(공유 링크, 레슨 트랙).

**거부한다:** 일급 함수, 상속, 모듈 시스템, 표준 라이브러리, 패키지 매니저.
전부 Pascal 이 간 길로 한 걸음씩 가는 것이다. 기능 판단 기준은 "있으면 좋은가"가 아니라
**"이게 없으면 교과서 알고리즘을 못 쓰는가"** 다.

이 기준은 양쪽으로 작동해야 의미가 있다. `"*" * n` 은 통과했다 — 별 찍기는 반복문 단원의 표준
과제이고, 그전까지는 Venos 로 세 줄이 걸렸다. 리스트의 멤버십과 위치도 통과했는데, 새 개념이
아니라 일관성 수리였다: `has` 는 딕셔너리 전용이고 `find` 는 문자열 전용이라 "리스트에 있나"에
답이 없었다. 반면 필드 이름으로 레코드를 정렬하는 기능은 거부했다 — 정렬 단원은 학생이 직접
정렬을 짜는 단원이고, 다른 데서는 그렇게 짠 것을 다시 쓰면 된다. 임의 정밀도 정수도 거부했다.
수 체계를 통째로 바꾸는 값을 치러야 하고, 수가 커졌을 때의 정직한 답은 `topython` 이다.

`topython` 쪽에서도 같은 종류의 판단이 한 번 있었다. 파이썬은 `xs[-1]` 을 "마지막 원소"로
읽으므로, Venos 의 1부터 세는 인덱스를 `xs[i-1]` 로 옮기면 **번호가 0 으로 내려갔을 때
에러 대신 엉뚱한 값이 조용히 나온다.** 모든 인덱싱을 검사하는 도우미 호출로 바꾸면 막히지만,
그러면 삽입 정렬이 `A[j-1]` 대신 `_at(A, j)` 로 나온다 — 읽히는 파이썬을 내는 것이
이 기능의 존재 이유인데 그걸 깎아 먹는다. 그래서 **숫자가 그 자리에 적혀 있으면
(`xs[0]`) 변환을 거절하고, 변수면 `xs[i-1]` 을 그대로 내되 생성된 파일 머리말에 적는다.**
파이썬이 다르게 답하는 나머지 자리들(`replace` 와 `find` 의 빈 문자열, `min`/`max` 의 문자열,
`substr` 의 0번 시작)은 읽기 쉬움을 잃지 않고 막을 수 있어서 전부 막았다 —
리터럴이면 거절하고, 아니면 검사하는 도우미로 보낸다. `tests/nopython/` 이 그 거절을 지킨다.

문자열과 숫자의 `+` 도 같은 길을 갔고, 이 중에서 제일 넓다. Venos 는 이어붙이고
(`"나이: " + 15` 는 `"나이: 15"`) 파이썬은 TypeError 다. PyGen 은 **한쪽이 문자열인 게 눈에
보일 때만** 반대쪽에 `_show` 를 씌우는데, 교과서 코드가 실제로 쓰는 모양은 거기 다 들어온다.
못 보는 건 타입이 실행할 때 정해지는 값이다 — 함수의 인자, 리스트의 원소, 딕셔너리의 값.
거기서 `더하기("가", 1)` 은 Venos 에서 돌고 파이썬에서 죽는다. 닫으려면 모든 `+` 를 도우미로
보내야 하는데 **예제집 전체에 258곳**이다 (`+` 212, `+=` 46 — 프로그램 열여덟 개로 다시 셌다). `합 + 점수[i]` 가 `_add(합, 점수[i])` 가
된다는 뜻이고, 그게 이 기능이 존재하는 이유인 읽히는 파이썬이다. 그래서 차이는 그대로 두고
명세에 적었으며, 생성 퍼저에게는 타입이 안 보이는 자리에서 둘을 섞지 말라고 일러 뒀다 —
적어 둔 차이가 CI 를 무작위로 빨갛게 만들면 안 되니까. **Venos 프로그램이 죽지 않고 도는데도
받아들인 유일한 차이**라, 한 곳이 아니라 세 곳에 적어 둔다.

연산자 둘도 같은 길이다. `리스트 * 2` 는 파이썬에서 `[1, 2, 1, 2]` 가 되고 `리스트 < 리스트`
는 `0` 을 돌려주는데, Venos 는 둘 다 에러다. 닫으려면 `*` 와 `<` 를 도우미로 감싸야 하고, 그건
교과서 코드에서 제일 흔한 연산자 둘이라 `i * 2` 가 `_mul(i, 2)` 로 나가게 된다 — 읽히는
파이썬을 통째로 내주는 값이다. 게다가 이 자리에 빠지려면 **애초에 Venos 에서 죽는 프로그램**을
들고 와야 한다. 그래서 열어 뒀다.

`upper()`·`lower()` 도 같은 길로 갔다. Venos 는 영문자만 바꾸고 나머지는 건드리지 않는데,
파이썬의 `str.upper()` 는 유니코드 전체를 훑어서 `café` 가 `CAFÉ` 가 되고, 터키어의 점 없는
`ı` 가 `I` 가 되고, 독일어 `ß` 는 `SS` — **한 글자가 두 글자로** 늘어난다. 둘 다 도우미로
감싸면 닫히지만, 그러면 학생이 `s.upper()` 라는 표기를 영영 못 만난다. 배우러 온 게 그건데.
한글·숫자·이모지는 양쪽 다 손대지 않으므로 이걸 쓰는 교과서 프로그램(시저 암호, 진법 변환)은
정확히 같은 답을 낸다. 그래서 차이는 그대로 두고 **생성 파일 머리말에 적는다** — 인덱스와 같은
거래, 같은 이유다.

규칙 하나는 반대로 갔고, 그 이유가 중요하다. `for i = a to b` 는 예전에 방향을 실행할 때
정했다 — `a > b` 면 내려가고 아니면 올라간다. 편의처럼 보이지만, 교과서가 늘 쓰는 그 반복문을
써 보면 아니다. **`for i = 1 to len(A)` 가 빈 리스트에서 0번이 아니라 두 번 돌았다** — `i = 1`
다음에 `i = 0` 인데, 여기서 0 은 인덱스가 아니다. **`examples/algorithms/` 의 정렬과 탐색이
원소 0·1개 리스트에서 전부 죽고 있었다.** 교과서의 `for i ← 1 to n` 도, Pascal 의 `for` 도,
파이썬의 `range` 도 답이 같다: `n < 1` 이면 안 돈다. 그래서 규칙을 버렸고, 거꾸로 세려면
반복문 레슨이 이미 가르치고 있던 `step -1` 을 쓴다. 이게 파괴적 변경이 아니라 싼 수정이 된
이유는 둘이다 — 저장소의 어떤 내려가는 반복문도 자동 추론에 기대지 않았고, 양끝이 상수로
적혀 있으면 파서가 이름을 불러 준다 (`for i = 10 to 1` → "거꾸로 세려면 step -1 을 붙이세요").
덤이 수정보다 컸다: 방향이 컴파일 시점에 정해지니 `topython` 이 도우미 호출 대신 진짜
`range(1, len(A) + 1)` 을 낸다 — 42곳 중 32곳에서.

대부분의 작업이 이 모양이다. **포지셔닝은 기능을 거부하기만 하는 게 아니라 무엇이 버그인지도
정한다.** 교과서 반복문을 경계에서 틀리게 만드는 편의는 적어 둘 절충이 아니라 결함이고,
그걸 보이게 만든 건 예제집이었다.

테스트 도구에도 같은 잣대를 대는데, 답이 다를 수 있다. `examples/rpg.my` 는 이 언어로 쓴 제일 큰
프로그램이다 — 222줄이고, 클래스·딕셔너리 디스패치·문자열 보간·파일 입출력이 한꺼번에 도는 유일한
자리다. 그런데 `random()` 을 쓴다는 이유로 differential 스위트 밖에 있었다. 실행할 때마다 답이
달라지는 프로그램은 백엔드끼리 비교할 수 없기 때문이다. 해법은 언어에 `seed()` 를 **더하지 않는**
것이었다. 교과서 알고리즘 중에 그게 없으면 못 쓰는 건 없고, 내장 함수 하나는 가르칠 것 하나이자
다시는 못 빼는 것 하나다. 대신 `random()` 이 환경변수 `VENOS_SEED` 를 읽고, 그걸 세우는 건 테스트
러너뿐이다. 언어 표면은 그대로고, 학생은 이런 게 있는 줄도 모르며, 대표 프로그램은 이제 푸시마다
검사된다. 난수를 피하려고 `tests/cases/` 에 두고 있던 **222줄짜리 통째 복사본** — 원본과 언제든
어긋날 수 있던 파일 — 도 같이 지웠다.

이번에 더한 것 하나는 기능처럼 보이지만 기능이 아니다. 한국 교과서는 학생에게 **추적표**를
손으로 그리게 한다 — 줄을 따라가며 변수가 무엇을 담고 있는지 적는 표이고, 그 대상이 바로
[`examples/algorithms/`](examples/algorithms/) 의 알고리즘들이다. Portugol Studio 가 변수
감시창이 딸린 디버거를 들고 있는 이유도 같다. Venos 에는 그게 없었다 — 프로그램은 도는데
값은 보이지 않는다. `venos trace`(플레이그라운드의 🔍 Trace)가 그 표를 찍는다. 값이 바뀔
때마다 한 줄, 부름은 `->`, 돌려줌은 `<-` 로 들여써서 재귀를 교과서가 그리는 모양으로 보여
준다. 기능 판단 기준은 **기능이 아니라는 점**으로 통과한다: 키워드도 내장 함수도 아니고,
프로그램을 읽는 데 새로 배울 것이 없으며, 쓸 수 있는 프로그램이 달라지지도 않는다 — 에러가
이미 보여 주는 호출 경로와 같은 갈래다. 비용은 가장 뜨거운 루프에서 3% 쯤이고(재 봤다),
러너가 규칙 하나로 붙잡는다: **`trace` 로 돌린 답이 안 돌린 답과 글자까지 같아야 한다.**
그 규칙이 1분 만에 자기 첫 버그를 찾았다 — 자기 자신을 품은 값을 찍다가 예외가 나서, 추적이
지켜보던 프로그램을 죽이고 있었다.

`topython` 안에서도 같은 판단을 한 번 더 했다. 프로그램에 딕셔너리가 하나라도 있으면 PyGen 은
리스트 번호와 딕셔너리 키를 구분할 수 없어서 `A[j]` 가 전부 `_idx(A, j)` 가 된다. 1500개짜리
선택정렬이 **딕셔너리 없는 파일에서는 0.06초, 딕셔너리 리터럴 한 줄만 더하면 0.60초**다 — 열 배고,
비용은 검사가 아니라 **호출 자체**다. 없애려면 `A` 가 리스트라는 걸 증명해야 하는데, 교과서 정렬에서
`A` 는 언제나 인자라 그 증명은 부르는 쪽까지 따라가야 한다. 증명이 틀리면 딕셔너리에 `d[k - 1]` 을
내보낸다 — **조용히 잘못 읽는 것**을 성능을 이유로 사들이는 셈이다. 예제집 열여덟 개는 어느 쪽이든
0.01~0.02초라, 이 언어가 쓰이는 크기에서는 그 차이가 존재하지 않는다. 재고, 적고, 안 고쳤다.

성능에서도 한 번 같은 판단을 했다. 같은 프로그램을 `topython` 으로 옮겨 CPython 과 나란히
재 보면 **반복문·리스트 3배, 재귀 6배 느리고 딕셔너리는 오히려 조금 빠른데**, 딱 하나
반복문 안의 `s = s + 글자` 가 **길이의 제곱**이다 (파이썬은 참조가 하나뿐일 때 제자리에서
늘린다). 대입 한 자리에 특수 처리를 넣으면 선형이 되지만 **넣지 않기로 했다** — 2만 자에
0.07초, 교과서가 닿는 어떤 크기에서도 보이지 않는 비용이고, 수가 커졌을 때의 정직한 답이
`topython` 인 것은 임의 정밀도 정수 때와 같다. 대신 명세에 실측값을 적어 뒀다. 재고 나서
안 고치기로 한 것과, 재 보지도 않은 것은 다르다.

## 7. 되고 있는지 어떻게 아는가

정직하고 확인 가능한 신호들. 대략 어려운 순서대로.

1. 만든 사람이 아닌 누군가가 Venos 프로그램을 짜고 거기에 `topython` 을 돌려 본다.
2. 선생님이 공유 링크로 시작 코드를 나눠 준다.
3. 교과서 알고리즘(선택 정렬, 이진 탐색)을 파이썬 환경 세팅하는 것보다 Venos 로 돌려 보는 게
   더 쉽다고 누군가 말한다.
4. 어느 교실이 블록→텍스트 전환 단원에 이걸 쓴다.

다운로드 수와 스타 수는 일부러 이 목록에 넣지 않았다.
