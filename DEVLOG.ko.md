# 내가 찾은 버그는 전부, 그걸 볼 수 있는 도구를 만든 날 나왔다

*[English](DEVLOG.md) | 한국어*

> 초안. [Venos](https://github.com/Vpdrla/Venos) — 교과서 의사코드처럼 읽히고 스스로를
> 파이썬으로 옮길 수 있는 작은 프로그래밍 언어 — 를 만들면서 적어 둔 것.

테스트에 관한 글을 쓰려던 게 아니었다. 언어를 만들면서 버그를 어떻게 찾았는지만 적어
뒀는데, 목록이 길어지고 나니 예상 못 한 게 보였다. **코드를 노려봐서 찾은 건 거의
없다.** 전부 "그 종류의 버그를 볼 수 있는 도구"를 만든 바로 그날 나왔고, 하루 전까지는
하나도 보이지 않았다.

도구가 도착한 순서대로 적는다.

---

## 1. 백엔드 둘, 같은 프로그램, diff

Venos 에는 인터프리터와 C++ 트랜스파일러가 있다. 언어 기능 하나를 두 번 구현한다는
뜻이고, 두 번째 구현은 첫 번째가 틀리지 않는 방식으로 틀릴 수 있다는 뜻이다.

그래서 테스트 스위트는 각 프로그램을 두 방식으로 돌려 출력을 diff 한다. "기대한 값이
나오는가"가 아니라 그냥 **"두 구현이 같은 말을 하는가"**. 정답을 적어 둘 필요가 없다.
의견을 둘 갖고, 갈라지는 순간을 알아채기만 하면 된다.

이게 코드젠 버그를 계속 잡아냈다 — 거기까진 예상대로다. 예상 밖이었던 건 **몇 달 동안
계속** 잡았다는 것이다. 기능을 하나 더할 때마다 대충 구현한 쪽 절반을, 대개 코드를 쓴 지
1분 안에 잡아냈다. 나중에 세 번째 백엔드(파이썬 생성기)가 diff 에 합류했는데, C++ 쪽보다
더 많이 걸렸다. 파이썬에는 파이썬의 고집이 있기 때문이다.

내가 아는 테스트 아이디어 중 가장 싸다. 유지할 기대 출력도, 쓸 단언문도 없고, 구현을
하나 더할 때마다 강해진다.

## 2. `IN` 과 `OUT`

윈도우의 `windows.h` 는 `IN` 과 `OUT` 을 **빈 매크로**로 정의한다. 내 토큰 enum 에는
`for x in xs` 의 그 `IN` 이 있었다.

이때 나오는 에러 메시지는 "IN 이라는 이름은 쓸 수 없습니다" 가 아니다. 진짜 원인에서
백 줄쯤 떨어진 자리부터 시작되는 문법 오류의 벽이다. 전처리기가 단어만 지우고 쉼표는
남겨 놨기 때문이다.

고치는 법은 enum 이름을 `INKW` 로 바꾸고 include 뒤에 `#undef IN` 을 넣는 것. 배운 건,
**버그가 내 코드 안에 없었다**는 점이다. 내 코드와 1985년에 쓰인 헤더 사이에 있었고, 내
파일을 아무리 들여다봐도 나오지 않았다. 윈도우에서 한 번 컴파일하니 한 번에 나왔다.

그러면 당연히 따라오는 질문이 있다. **윈도우는 그 밖에 또 무엇을 하고 있었을까?** 답은
한참 뒤에 나온다.

## 3. 사실은 재귀 한도였던 세그폴트

깊은 재귀가 죽었다. "에러를 냈다"가 아니라 메시지도 없이 죽었다.

인터프리터는 C++ 스택 위에서 재귀한다. Venos 호출 하나당 수 KB. 리눅스는 스레드에 기본
8MB, 윈도우는 2MB 를 준다. 내가 감으로 정한 재귀 한도 2000 은 실제 스택이 바닥나는 지점
**위**에 있었고, 그래서 한도는 한 번도 걸리지 않았다.

마음에 드는 해결책은 이거였다: **128MB 스택을 가진 전용 스레드**에서 프로그램을 돌린다.
이제 언어 자신의 한도가 항상 먼저 걸리고, 무한 재귀를 쓴 학생은 세그폴트 대신 한국어
문장을 본다.

나중에 같은 버그가 다른 모자를 쓰고 돌아왔다. 브라우저의 WebAssembly 빌드에는 스레드가
없고 — 이걸 깨닫는 데 부끄러울 만큼 오래 걸렸다 — Emscripten 의 `TOTAL_STACK` 은
**선형 메모리 안의 그림자 스택** 크기지 자바스크립트 엔진의 호출 스택이 아니다. 엔진
스택은 훨씬 얕고, 학생은 `RangeError: Maximum call stack size exceeded` 를 본다. 영어로.
에러가 읽힌다는 게 존재 이유인 언어에서.

실제로 어디서 깨지는지 재 보니 이상했다. 450단계가 두 번 통과하고 나서 380단계가
실패했다. 넘어가는 지점이 실행마다 다르고, 한 번 넘치면 그 페이지는 회복되지 않는다.
그래서 웹 한도는 200이다. 내가 본 모든 실패보다 한참 아래고, 교과서가 요구하는 어떤
재귀보다도 한참 위다 — 하노이·유클리드·이진 탐색은 전부 30단계 미만이다.

## 4. 예외는 공짜가 아니고, `return` 은 드물지 않다

`fib(27)` 이 2.7초 걸렸다. CPython 은 0.039초다.

CPython 보다 느린 건 괜찮다. 트리워킹 인터프리터가 바이트코드 VM 상대로 몇 배 느린 건
원래 그런 거래다. 하지만 **70배는 그 거래가 아니라 버그**다.

원인을 떼어내는 실험은 2분 걸렸다. 같은 재귀를 `return` 없이 쓰면 0.25초에 끝났다.
`return` 이 C++ 예외로 구현돼 있었다. **모든 호출마다** 던지고 받았다.

예외는 안 일어날 때 싸다. `return` 은 언제나 일어난다.

이걸 `Flow { NORMAL, BREAK, CONTINUE, RETURN }` 반환값으로 바꾸고 모든 문장에 전파시키는
데 하루 오후가 걸렸다. 그리고 `try` 안의 `break`, 반복문 안의 `try`, 메서드 안의 그 전부를
담은 고약한 테스트 파일이 필요했다. 같은 기계에서 직전 커밋과 비교:

| | 전 | 후 | CPython |
|---|---|---|---|
| `fib(27)` | 2.73초 | **0.23초** | 0.039초 |
| 하노이 20단 | 10.58초 | **1.93초** | — |
| 300만 번 루프 | 0.222초 | 0.215초 | 0.234초 |

마지막 줄이 **대조군**이다. 거기선 아무것도 빨라지지 않았다. 던지는 게 없었으니까. 그
줄이 없었으면 이 숫자들은 내가 나한테 들려주는 이야기였을 것이다.

CPython 의 6배는 원래 그런 거래다. 그 거래는 받는다.

## 5. 윈도우 러너를 켠 날

릴리스 워크플로는 한동안 윈도우 `.exe` 를 내보내고 있었다. 아무도 한 번도 실행해 본 적이
없었다. 리눅스에서 크로스 컴파일해서 그대로 올렸을 뿐이다.

MinGW 로 빌드해 전체 스위트를 돌리는 `windows-latest` 잡을 붙였다. **첫 네 번의 실행에서
진짜 버그 네 개**가 나왔고, 전부 리눅스에서는 보이지 않는 것들이다:

1. **한글 파일 이름이 안 됐다.** 윈도우는 `main()` 의 argv 를 ANSI 코드페이지로 준다.
   그래서 `venos 정렬.my` 가 `venos ??.my` 로 도착한다. 한국어 사용자가 한국어로 이름을
   지을 수 있다는 게 내세운 점인 언어에서, 윈도우 빌드는 한국어로 지은 파일을 못 열었다.
2. **트랜스파일한 바이너리도 한글 경로를 못 열었다** — 인터프리터는 넓은 문자로 변환하는
   자리에서 좁은 `std::ifstream` 을 쓰고 있었다.
3. **빌드본에서는 모든 파일 판정이 참이었다.** `std::filesystem::path` 로
   `std::ifstream` 을 만들면 MinGW 에서 **없는 파일도 열렸다고 답한다.** 그래서
   `exists()` 가 뭐든 예라고 했고 `readfile()` 은 에러를 안 냈다.
4. **생성된 파이썬이 한글 출력에 죽었고**, 그다음엔 한글 입력을 뭉갰다. 윈도우 파이썬이
   stdout 을 로케일 코드페이지로 인코딩하기 때문이다.

버그 넷, 전부 같은 자리 — **내 UTF-8 세계와 플랫폼의 경계**. 몇 주 동안 릴리스로 나가고
있었다. 러너는 YAML 스무 줄이었다.

### 다섯 번째는 러너가 **보고도 통과시켰다**

한참 뒤, 전혀 다른 걸 고치다가 다섯 번째가 나왔다. 인터프리터가 **잡히지 않은 에러에도 종료
코드 0** 을 돌려주고 있었다 (같은 프로그램의 빌드본은 1 을 돌려줬다). 채점 스크립트라면 죽은
프로그램을 통과로 읽는다. 고쳐서 밀었더니 윈도우 잡이 처음으로 빨간불이 됐다.

그 잡에는 `tests/.tmp_한글 폴더/정렬.my` 를 세 경로로 돌리는 단계가 **이미 있었다**. 그리고
`venos build` 단계는 **계속 실패하고 있었다** — `cc1plus: fatal error: í•œê¸€ ...: No such
file or directory`. g++ 의 argv 도 ANSI 코드페이지로 변환돼 들어가니 당연한 일인데, `cmdBuild`
가 무슨 일이 있어도 0 을 돌려주는 바람에 CI 로그에는 에러 문구가 찍히고 단계는 초록이었다.

그러니까 이 버그는 **도구가 없어서** 숨은 게 아니다. 도구는 있었고, 매번 돌았고, 정확히 그
자리를 가리키고 있었다. 신호를 0 으로 뭉개 놓았을 뿐이다. 앞 아홉 장의 교훈이 "다음 도구를
만들어라"였다면 이 장의 교훈은 그보다 싸다: **이미 가진 도구가 실패를 실패라고 말할 수 있는지
확인하라.** 초록불은 통과한다는 뜻이지 봤다는 뜻이 아니다.

(고친 방식: 한글 경로면 그 폴더로 들어가 ASCII 임시 이름으로만 g++ 를 부르고 결과를 제자리
이름으로 옮긴다. g++ 를 내가 고칠 수는 없으니 argv 에 한글을 넣지 않는 쪽으로 돌아간 것이다.
CI 단계는 이제 빌드한 프로그램이 실제로 계산한 값을 냈는지까지 본다.)

## 6. 그게 내놓는 파이썬은 다른 프로그램이었다

`venos topython` 은 Venos 프로그램을 파이썬으로 다시 쓴다. 이 언어가 종착지가 아니라
다리라고 말할 수 있는 근거다 — **나가는 길을 직접 파는 언어는 슬그머니 머무는 곳이 될 수
없다.**

그래서 **틀린** 파이썬을 내는 게 이 기능이 할 수 있는 최악의 일이다. 거절은 정직하다.
그럴듯한데 다르게 답하는 번역은 함정이다. 학생은 그게 틀렸다는 걸 알 길이 없으니까.

생성된 파일을 읽어서 넷을 찾았다:

```
Venos 가 찍는 것          생성된 파이썬이 찍은 것
평균: 83.3333             평균: 83.33333333333333
값: 1                     값: True
딕: {"a": 1}              딕: {'a': 1}
-7 % 3 → 2                -1
```

`print` 는 값을 서식 도우미로 보냈는데 f-string 은 안 보냈다. 그리고 `%` 가 C 의 `fmod`
를 썼다. 왼쪽 피연산자의 부호를 따르는 그 함수 — 그래서 **음수를 한 바퀴 돌리려고
존재하는 교과서 예제인 시저 암호**가 백엔드에 따라 다른 답을 냈다.

거기서 읽기를 멈추고, 같은 경계 케이스를 세 백엔드에 넣고 diff 하는 프로브를 짰다. 다섯
개가 더 나왔고 이쪽이 더 고약했다. 전부 **Venos 는 에러를 내는데 파이썬은 조용히 값을
돌려주는** 자리였기 때문이다:

| Venos | 생성된 파이썬 |
|---|---|
| `xs[0]` → 에러, 인덱스는 1부터 | **마지막 원소** |
| `replace(s, "", r)` → 에러 | `-a-b-c-`, 글자 사이마다 끼워 넣음 |
| `find(s, "")` → 에러 | `1` |
| `substr(s, 0, n)` → 에러 | `""` |
| `min("a", "b")` → 에러 | `"a"` |

`xs[0]` 이 제일 마음에 걸린다. "인덱스는 1부터"는 명세의 초보자 최빈 실수 목록 **1번**
이다. 학생이 `xs[0]` 이라고 쓸 거라는 걸 우리가 알고 있었다는 뜻이다. 그런데 번역이
잡히던 실수를 틀린 답으로 바꿔 놓았다. 파이썬은 음수 인덱스를 뒤에서 세는 뜻으로 읽으니까.

고친 모양에는 이름을 붙일 만하다. **문제의 값이 소스에 적혀 있으면** `topython` 이 줄
번호를 대고 거절한다. **변수면** 실행 시점에 Venos 의 규칙을 강제하는 도우미를 거친다.
그런데 변수 **인덱스**만은 그대로 `A[i - 1]` 을 낸다. 안 그러면 삽입 정렬이

```python
while j >= 1 and A[j - 1] > 키:
    A[(j + 1) - 1] = A[j - 1]
```

에서 `_at(A, j)` 호출로 뒤덮인 한 페이지가 되는데, **학생이 읽을 수 있는 파이썬을 내는
것이 이 기능의 존재 이유** 전부다. 그래서 생성 파일 머리말에 그 한계를 적었다.
모든 구멍을 막아야 하는 건 아니다. 어떤 구멍에는 표지판을 세운다.

## 7. 퍼저, 그리고 나가는 길에서만 나던 크래시

학생은 오타를 낸다. 끊임없이. 그래서 코퍼스에 작은 뮤테이션 퍼저를 겨눴다 — 바이트를
뒤집고, 조각을 지우고, 중괄호를 흘리고, 여는 괄호를 3천 개 쌓는다 — 그리고 AddressSanitizer
와 UndefinedBehaviorSanitizer 를 켠 빌드에 돌렸다.

새니타이저는 **아무것도 못 찾았다.** 코퍼스 전체를, 인터프리터로도, 새니타이저를 켜서
따로 컴파일한 **생성 C++** 로도. 기분 좋은 날이었다.

퍼저는 하나를 찾았고, 그게 좋은 놈이었다. **재귀 하강 파서에 깊이 제한이 없다.**
`print ((((((…` 가 스택이 다할 때까지 재귀한다.

재미있는 건 **어디서** 죽었는가다. 파일을 실행하는 건 수만 단계까지 멀쩡했다. §3 의
128MB 스택 스레드 덕이다. 그런데 `topython` 과 `build` 는 메인 스레드에서 파싱하고,
5천 단계에서 죽었다. **같은 파일이 실행은 되는데 변환만 죽었다.**

파서에 깊이 제한 하나를 넣으니 전부 해결됐고, 예상보다 더 해결됐다. 파스 깊이를 막으면
AST 깊이가 묶이고, 그러면 평가기·코드 생성기·파이썬 생성기·AST 소멸자의 재귀까지 같이
묶인다. **다섯 대신 하나.**

같은 입력에서 두 개가 더 떨어졌다. 에러 밑에 찍는 소스 줄이 잘리지 않아서, 괄호 5천 개
케이스가 197KB 를 쏟고 정작 읽어야 할 메시지를 화면 밖으로 밀어냈다. 그리고 문자열 보간
안의 식은 줄 번호가 1부터 다시 시작하는 하위 파서가 파싱해서, 4번 줄의
`print "{없는변수}"` 에서 난 에러가 1번 줄을 가리켰다.

## 8. 테스트가 **안 보고 있는 것**을 물어보기

한참 늦게, 훨씬 먼저 했어야 할 일을 했다. 테스트를 더 쓰는 대신 **어떤 기능에 테스트가
없는지**를 물었다.

`import` 에는 없었다. 하나도. differential 스위트의 어떤 프로그램도 import 를 쓰지 않았다.

그래서 하나 썼더니 바로 실패했다. 미묘한 버그 때문이 아니라, `import "utils.my"` 를
**import 를 쓴 파일 옆이 아니라 현재 작업 폴더에서** 찾고 있었기 때문이다. 프로젝트 폴더는
마침 그 안에 서 있을 때만 작동했다:

```
$ venos 프로젝트/main.my
!! 에러: import 실패: 파일을 열 수 없습니다: utils.my
```

`utils.my` 는 `main.my` 바로 옆에 있는데. 모든 언어가 import 를 그 파일 기준으로 푼다.
내 것은 한 번도 그런 질문을 받은 적이 없었을 뿐이다.

같은 감사에서 채움말·이스케이프·중첩 경로 대입·리스트 안의 객체·나중에 붙인 필드·리터럴
끝 쉼표·연산자 우선순위를 덮는 케이스가 하나 나왔고, 세 백엔드가 이미 모든 줄에서
일치했다. 원하던 결과다. 그리고 **확인하기 전까지는 아무 가치도 없었다.**

커버리지 감사는 이미 테스트하는 것을 더 테스트하는 것보다 훨씬 좋은 비율로 버그를 찾는다.
불편한 이유는, 질문이 "내가 뭘 피해 왔지?" 라서다.

## 9. 에러, 혹은 초보자가 실제로 치는 것

이 언어는 블록 코딩에서 파이썬으로 건너가는 학생을 겨냥한다. 즉 학생은 **다른 언어의
습관을 안고 도착한다.** 주로 파이썬의 습관이고, 어차피 배우러 가는 중인 그 언어의 것이다.

그런 학생이 칠 법한 스니펫 스무 개를 쳐 보고 돌아오는 걸 읽었다:

```
print 2 ** 3        →  문법 오류: 값이 와야 할 자리입니다
// 주석             →  문법 오류: 문장이 될 수 없는 토큰입니다
print !true         →  알 수 없는 문자: '!'
} elif x == 2 {     →  문법 오류: = 이(가) 필요합니다
print True          →  정의되지 않은 변수: True
"abc".upper()       →  문자열에는 메서드를 호출할 수 없습니다
```

전부 사실이고, **어느 것도 열세 살에게 대신 뭘 치라고 말해 주지 않는다.** 그중 둘
(`elif`, `!`)은 명세의 "자주 하는 실수" 목록에 **문자 그대로 적혀 있다.** 일어날 걸 알면서
막상 일어났을 때 쓸모 있는 말을 안 한 것이다.

지금은 습관의 이름과 대체물을 같이 말한다. *"거듭제곱 연산자는 없습니다 — x * x 로 쓰거나
반복문으로 곱하세요"*, *"주석은 // 가 아니라 # 로 씁니다"*, *"elif 는 없습니다 — else if 로
쓰세요"*. 조회표 하나와 렉서 분기 몇 개다. 하루 오후 걸렸다. 어려운 건 **보기로 마음먹는
것**뿐이었다.

여기엔 그냥 못 박아 둘 만한 버전이 있다. **에러 메시지는 기능이다. 기능처럼 테스트하라.**
스위트에는 일부러 틀린 프로그램이 서른다섯 개 있고, 출력이 기록된 파일과 글자까지 같아야
한다. 문구를 고치면 그 파일도 고쳐야 하는데, 그 마찰이 정확히 내가 문구를 함부로 바꾸지
못하게 한다.

## 10. 같은 주에 만든 열 번째 도구

§8 을 쓰다 보니 모양이 너무 분명해져서, 글을 끝내기 전에 다음 도구를 만들러 갔다.

§7 의 퍼저는 진짜 프로그램을 망가뜨리기 때문에 내놓는 것의 거의 전부가 문법 오류다.
크래시는 찾을 수 있어도 **틀린 답**은 못 찾는다. 그래서 반대쪽을 만들었다. 문법에서
**올바른** Venos 프로그램을 무작위로 생성하는 도구 — 중첩된 if, `try` 안의 `break` 와
`continue`, 리스트 인덱스 안의 문자열 곱하기, 딕셔너리 순회, 생성자와 메서드가 있는
클래스 — 를 세 백엔드에 돌리고 출력을 diff 한다.

생성한 프로그램은 **에러 없이 끝나도록** 제약한다. 에러 문구는 백엔드마다 다를 수 있어서
거기까지 비교하면 거짓 실패만 잔뜩 나온다. 그러고 남는 것이 정작 중요한 질문이다 —
**돌아가는 프로그램에서 세 구현이 같은 답을 내는가.**

일곱 번째 프로그램이 아니라고 했고, 그것도 **세 갈래**로 갈렸다:

```
try { 없는변수 = 3
      print "여기 안 옴" } catch 오류 { print "잡음" }
```

인터프리터는 그 줄이 돌 때 에러를 내고 `catch` 가 잡는다. `venos build` 는 설계대로
빌드 시점에 거절한다. 그리고 `topython` 은 **변수를 그냥 대입하는** 파이썬을 냈다.
파이썬에서는 대입이 이름을 만들기 때문이다. 이 언어가 원래 잘 잡아 주던 실수 하나로
다른 프로그램이 된 것이다.

아홉 번째 프로그램은 멈추지 않았다 — **파이썬이** 멈추지 않았고 Venos 는 끝났다:

```
let xs = [1, 2, 3]
for x in xs { push(xs, x * 10) }
```

Venos 는 루프에 들어갈 때의 리스트를 돈다. 파이썬의 `for` 는 살아 있는 리스트를 돌아서
방금 붙인 원소까지 돌고, 영원히 돈다. 에러는 어디에도 없고 답만 다르다. 그것도 **아무
잘못이 없는 프로그램**에서. 순회 중인 리스트에 덧붙이는 건 초보자가 일부러 하는 일이다.

이쪽은 고친 방식이 마음에 든다. 이제 생성기가 **루프 몸통을 먼저 만들어** 보고, 그
안에서 그 이름이 나올 때만 `list(...)` 로 감싼다 — 알고리즘 예제 16개에서 감싸진 루프는
**0개**다. `for 점수 in 점수들:` 이 그대로 남는 게 이 기능의 존재 이유 전부다.

### 그리고 한 번 더, 같은 날

며칠 뒤 손으로 내장 함수를 찔러 보다 두 개를 더 찾았다. `has("abc", "b")` 는 Venos 에서
에러, 파이썬에서 `1`. `sort([[2],[1]])` 도 똑같이 에러와 답. 둘 다 고치고 나서야 뒤늦게
생각이 났다 — **손이 찾을 수 있는 종류면 생성기도 찾을 수 있어야 한다.**

그래서 생성기에 줄 하나를 더 만들게 했다: 내장 함수 열아홉 개에, 일부러 규칙에서 벗어난
인자 스물두 가지(빈 문자열, 중첩 리스트, `"0x10"`, 딕셔너리가 와야 할 자리의 리스트…)를
섞어 넣고 `try/catch` 로 감싸 `"E"` 한 글자로 접는 줄이다. 문구는 안 보면서 "에러인가
값인가" 만 비교된다. 생성 프로그램을 늘 에러 없이 끝나게 묶어 둔 제약을, 딱 여기서만 푼 것이다.

**열다섯 번째 프로그램에서** `join("abc", "-")` 이 나왔다. 파이썬의 `str.join` 은 인자를
순회하고 문자열은 글자 단위로 돌기 때문에, Venos 가 거절하는 호출이 `"a-b-c"` 를 들고
돌아온다. 같은 표를 손으로 훑으면서 내가 그냥 지나친 것이다.

덤으로, 새 도구는 새 거짓 양성을 함께 데려왔다. 생성기가 `max([[1],[2]], "1e3")` 같은 줄을
만들자 `topython` 이 **설계대로** 거절했고, 비교 스크립트는 그걸 어긋남으로 셌다. CI 가
빨개졌고 어긋남 세 건이 전부 그거였다. 같은 날 `fuzz.py` 에서 "모든 타임아웃을 멈춤으로
센" 것과 정확히 같은 실수다 — **도구를 켜기 전에는 그 도구가 뭘 잘못 볼지도 안 보인다.**

버그 둘, 둘 다 그때까지 만든 어떤 도구로도 볼 수 없던 종류였다. 이 글의 주장이 정확히
제때 도착한 셈인데, 계획했던 척하느니 그렇게 됐다고 적는 편이 낫겠다.

---

## 11. 이미 있던 도구에, 한 번도 안 한 질문

열한 번째 도구는 만든 게 아니다. 이미 있던 것에 물어본 적 없는 질문을 한 것이다.

교과서 알고리즘 예제집 열여섯 개는 한참 전에 스위트에 들어와 있었다. 세 방식으로 돌려
출력을 비교하고, 푸시마다 초록불이었다. 그런데 그것들이 받는 입력은 내가 적어 둔 것뿐이었다.
`선택정렬([64, 25, 12, 22, 11])` 은 열 번쯤 봤다. **`선택정렬([7])` 은 한 번도 안 봤다.**

    !! 에러: [줄 6] 인덱스 범위 초과: 2 (리스트 크기: 1, 인덱스는 1부터)

정렬·탐색 **일곱 개가 전부** 원소 하나짜리 리스트에서 죽었다. 원인은 하나였고, 알고리즘에
있지도 않았다. `for i = 1 to n - 1` 에서 `n` 이 1 이면 범위가 `1 to 0` 이 되는데, Venos 의
`for` 는 시작이 끝보다 크면 **방향을 뒤집도록** 만들어져 있었다. 편의라고 생각하고 넣은
줄이다:

    stepv = (s.num <= e.num) ? 1 : -1;

그래서 `for i = 1 to len(A)` 가 빈 리스트에서 0번이 아니라 **두 번** 돌았다 — `i = 1`,
그다음 `i = 0`. 여기서 0 은 인덱스가 아니다. 교과서의 `for i ← 1 to n` 도, Pascal 의
`for` 도, 파이썬의 `range` 도 `n < 1` 이면 안 돈다. 나만 달랐고, 내 예제집 전부가 그
차이 위에 서 있었다.

고치는 건 세 줄이었다. 고칠지 말지가 어려웠다 — 언어 의미를 바꾸는 일이니까. 결정을 쉽게
만든 건 두 가지 사실이었다. 저장소 안의 **내려가는 반복문은 이미 전부 `step -1` 을 쓰고
있었고**(자동 추론에 기댄 코드가 한 줄도 없었다), 반복문 레슨은 이미 "거꾸로 세려면 `step -1`
처럼 음수를 씁니다" 라고 가르치고 있었다. 아무도 안 쓰는 편의가 모든 교과서 알고리즘을
경계에서 틀리게 만들고 있었던 것이다.

덤이 수정보다 컸다. 방향이 컴파일 시점에 정해지니 `topython` 이 도우미 호출 대신 진짜
`range()` 를 낼 수 있게 됐다 — `_rng` 가 42곳에서 10곳으로 줄고, `for i = 1 to n - 1` 이
`range(1, n)` 으로 나간다. 파이썬 교과서에 적힌 그 모양이다.

그러고 나서 같은 질문을 나머지 열여섯 개에 전부 던졌다. 모든 함수를 0, 1, 빈 값, 음수,
정의역 바로 바깥으로 걸어 봤다. 열 개가 틀린 답을 했다.

`진법으로(7, 1)` 은 **영원히 돌았다** (`n % 1` 은 0 이고 `n / 1` 은 `n` 이라 루프가 한
걸음도 못 나간다 — 교과서가 "2 ≤ 밑" 이라고 적어 두는 이유다). `체(0)` 은 빈 리스트의
`남음[1]` 을 읽었다. `전치([])` 는 `len(A[1])` 을 읽었다. `횟수(-3)` 은 `n == 1` 바닥을
지나쳐 재귀 한도까지 내려갔다.

죽는 것보다 나쁜 것도 셋 있었다. `곱하기(2×3, 2×3)` 은 **그럴듯한 2×3 영행렬을 돌려줬다**
— A 의 열 수를 B 의 행 수와 맞춰 보는 검사가 없었다. `진법으로(-26, 2)` 는 `""` 를 줬다.
`십진으로("9", 2)` 는 `9` 를 줬다. 2진수에 9 는 없다.

그리고 셋은 **자기 자신과** 어긋났다. `시저(시저("Hello", 5), -5)` 가 `"hello"` 였다 —
복호화를 보여 주는 파일인데 모든 글자가 `lower()` 를 통과해 소문자로 돌아왔다.
`횟수(0)` 은 죽고 `횟수_반복(0)` 은 0 을 줬는데, 둘은 같은 수를 세는 함수다.
`최대공약수(0, 5)` 는 5, `무식하게(0, 5)` 는 1 — 같은 파일 안에서.

마지막 하나는 버그가 아니라 알고리즘의 성질이라 고치는 대신 **소리 내어 적었다**: 런길이
압축은 숫자가 든 글을 되돌릴 수 없다. `압축("a1a")` 은 `"a111a1"` 이고, 되돌리면 112글자다.
그 파일은 이미 왕복을 스스로 검사하고 있었는데, 예시 다섯 개에 숫자가 하나도 없었다.

이제 열여섯 개 파일이 전부 `=== 경계값 ===` 절로 끝난다. 틀렸던 그 호출이 거기 들어가 있고,
3중 비교가 그걸 붙잡는다. 예제집은 장식이 아니라 **"교과서 알고리즘이 여기서 돈다"는 명세**고,
명세는 자기가 실제로 실행하는 것까지만 명세한다.

## 목록이 말하는 것

도구 아홉 개, 그리고 각각이 **오직 그것만 볼 수 있는** 종류의 버그를 찾았다:

| 도구 | 찾은 것 |
|---|---|
| 백엔드 둘 diff | 코드젠 버그, 계속해서 |
| 세 번째 백엔드 | 약속이 조용히 깨지고 있던 것 |
| 윈도우에서 컴파일 | 1985년 헤더, 그리고 UTF-8 경계 버그 넷 |
| 큰 스택 + 진짜 한도 | 크래시가 문장으로 바뀜 |
| 대조군이 있는 벤치마크 | `return` 하나에 마이크로초가 들던 것 |
| 퍼저 + 새니타이저 | 중첩 괄호에 세그폴트 나던 파서 |
| CI 안의 진짜 브라우저 | 한국어가 있어야 할 자리의 자바스크립트 에러 |
| 터치 뷰포트 | 태블릿 교실의 34px 버튼 |
| 커버리지 감사 | `import` 가 한 번도 테스트된 적 없다는 것 |
| 올바른 프로그램 생성기 | 에러 하나 없이 답만 달라지던 두 자리 |

패턴은 "테스트를 더 하라"가 아니다. **각 도구는 한 종류만 보고 나머지에는 눈이 멀었으며,
사각지대는 추론으로 도달할 수 없다**는 것이다. 다음 도구를 만들어 보는 수밖에 없다. 파서를
백 번 읽어도 깊이 제한이 없다는 걸 못 알아챘을 것이다. 코드 어디에도 "이게 빠졌다"고 적혀
있지 않으니까. 퍼저는 망가뜨린 프로그램 쉰 개 안에 말했다.

따라오는 결론은 덜 마음에 든다. **지금 Venos 에서 망가져 있는 것은 내가 지금 볼 수 없는
방식으로 망가져 있고, 그걸 찾는 방법은 더 열심히 보는 게 아니다.** 다음 도구를 만드는
것이다.

5장의 덧글은 그 결론의 값싼 단서다. 도구를 만들었다고 끝이 아니다 — **실패를 실패라고
말할 수 있는지**까지 봐야 한다. 초록불 하나가 한 해 가까이 에러 문구 위에 덮여 있었다.

10장이 바로 그 결론을 글이 열려 있는 동안 시험한 기록이다. 바랄 수 있는 가장 설득력 있는
형태였고, 가장 덜 의도한 형태이기도 했다. 도구 열 개, 눈먼 방식 열 가지, 그리고 열 번째가
아홉 개로는 볼 수 없던 것 둘을 찾았다.

그리고 11장이 그 결론을 한 번 더, 반대쪽에서 뒤집었다. 열한 번째는 **도구가 아니었다.**
이미 일 년 가까이 초록불을 내던 예제집에, 물어본 적 없는 질문 하나를 한 것이다 — 원소가
하나면 어떻게 되는데? 열 개가 틀린 답을 했고, 그중 하나는 언어 자체의 규칙이었다.

그러니 목록의 교훈을 한 줄 고쳐 적어야겠다. 새 도구를 만드는 것만이 길은 아니다. **가지고
있는 도구에 아직 안 물어본 입력이 무엇인지** 세어 보는 것도 길이다. 내 예제집은 내가 적어
둔 입력만 받아 왔고, 그 목록에 `[]` 도 `[7]` 도 없었다. 없다는 사실은 코드 어디에도 적혀
있지 않았다.

열두 번째는 내가 만들 수 있는 게 아니다. **아직 내가 아닌 누구도 Venos 프로그램을 써 본
적이 없다.**
