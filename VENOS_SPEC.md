# Venos 언어 명세 (v0.6.0)

*[English](VENOS_SPEC.en.md) | 한국어*

AI에게 Venos 코드를 짜달라고 할 때 이 문서를 함께 주면 됩니다.
소스 파일 확장자는 `.my`, 인코딩은 UTF-8.

## 실행 방법
```
venos 파일.my              # 인터프리터로 바로 실행
venos build 파일.my        # C++ 로 변환 후 g++ 컴파일 → 네이티브 실행 파일
venos build 파일.my run    # 빌드 후 바로 실행
venos topython 파일.my     # 같은 프로그램의 파이썬 버전을 만든다 (.my → .py)
venos trace 파일.my        # 실행하면서 값이 바뀔 때마다 한 줄씩 보여 준다 (추적표)
venos                      # 대화형 셸 (create/choose/code/run/build/...)
venos --help               # 사용법,  venos --version 은 버전
```
셸에서 `repl` 을 입력하면 한 줄씩 실행하는 REPL 시작 — 식만 입력하면 값을 바로 보여줌.
블록·리스트·괄호가 열려 있으면 `..` 로 이어 받고, 그만두려면 **빈 줄 두 번**, 나가려면 `quit`.

`trace` 는 교과서가 손으로 그리게 하는 **추적표**를 대신 찍어 준다. 값이 바뀔 때마다
`줄| 변수 = 값` 한 줄이 나오고, 함수를 부르면 `->`, 값을 돌려주면 `<-` 로 깊이만큼
들여쓴다 (플레이그라운드의 🔍 Trace 버튼도 같은 것이다):
```
    5| -> 팩토리얼(4)
    3|   -> 팩토리얼(3)
    3|   <- 팩토리얼 = 6
    5| <- 팩토리얼 = 24
    5| 답 = 24
```
추적 줄은 **stderr** 로 나가므로 `venos trace 파일.my 2>/dev/null` 이면 프로그램 출력만
남고, `2>추적.txt` 로 따로 받을 수도 있다. 인터프리터에서만 되고(에러의 호출 경로와 같다),
값이 500번 넘게 바뀌면 그 뒤로는 세기만 한다.

종료 코드는 성공이 0, 실패가 1이다 (잡히지 않은 에러, 입력이 끊김, `topython` 이 거절한 프로그램,
없는 파일, 모르는 인자). 채점 스크립트나 Makefile 에서 그대로 쓸 수 있다.

## 타입
숫자(정수/소수 구분 없음), 문자열, 리스트, 딕셔너리, 객체(클래스 인스턴스).
숫자는 실수 하나로 다루므로 **정수는 약 9천조(2^53)까지 정확**하다 — `팩토리얼(20)` 은 정확하고 `팩토리얼(25)` 은 어림수가 된다. 그보다 큰 수가 필요하면 `topython` 으로 옮기면 된다 (파이썬 정수에는 한계가 없다).
`true`/`false` 는 숫자 1/0. 참/거짓 판정: 0, 빈 문자열, 빈 리스트/딕셔너리가 거짓. 객체는 항상 참.
리스트/딕셔너리/객체는 **참조 방식** — 변수 복사나 함수 인자 전달 시 같은 것을 가리킴 (Python과 동일).
문자열은 **불변** — `s[1] = "x"` 불가, `replace()` 로 새 문자열을 만들어 재대입.

## 변수와 대입
```
let x = 10          # 선언 (초기값 생략하면 0)
let 이름 = "미르"    # 한글 변수명 가능
x = 20              # 대입 (let 으로 먼저 선언 필수)
x += 1              # += -= *= /= 지원 (리스트 원소, 객체 필드에도 가능)
```

## 출력 / 입력
```
print "안녕"                     # 자동 줄바꿈
print "x =", x, "끝"             # 쉼표로 여러 값 (공백으로 구분)
print "1줄\n2줄\t탭 \"따옴표\""   # 이스케이프: \n \t \" \\
print "이름: {이름}, 내년: {나이 + 1}살"   # 문자열 보간 — {} 안에 아무 식이나
let 답 = input "질문: "          # 숫자를 입력하면 자동으로 숫자 타입
```
보간 규칙: 진짜 중괄호는 `{{` / `}}`. 보간 식 안에 문자열을 쓰려면 따옴표를 `\"` 로 적는다 — `"{join(xs, \", \")}"`. 읽기 어려우면 변수로 빼는 편이 낫다.

## 연산자 (우선순위 높은 순)
```
- (음수)  →  * / %  →  + -  →  == != < > <= >=  →  not  →  and  →  or
```
- `+` 는 문자열이 섞이면 이어붙이기 (`"나이: " + 15` → "나이: 15"), 리스트끼리면 연결 (`[1] + [2]` → [1, 2])
  - **`topython` 의 한계 하나**: 한쪽이 문자열인 게 코드에서 보일 때만 파이썬 쪽에 `str()` 을 씌운다.
    함수의 인자·리스트의 원소처럼 타입이 실행할 때 정해지는 자리에서 둘을 섞으면, Venos 는 이어붙이지만
    옮긴 파이썬은 `TypeError` 로 멈춘다. 섞어 쓸 자리라면 `str(수)` 로 직접 감싸는 편이 안전하다.
- `*` 는 문자열 × 숫자면 반복 (`"*" * 5` → `"*****"`) — 별 찍기, 막대그래프, 구분선에 쓴다. 횟수는 정수, 0 이하면 빈 문자열
- `==`/`!=` 는 모든 타입 가능 — 타입이 다르면 그냥 false/true, 리스트/딕셔너리/객체는 **내용으로 깊은 비교**. `< > <= >=` 는 숫자/문자열만
- 비교 연산은 연결 불가 — `a < b < c` 대신 `a < b and b < c`
- `%` 의 결과는 **나누는 수의 부호**를 따른다 (수학·파이썬 관례) — `-7 % 3` 은 `2`, `-1 % 26` 은 `25`. 시저 암호처럼 음수를 되감을 때 그대로 쓸 수 있다
- `and`/`or` 는 단락 평가
- `#` 부터 줄 끝까지 주석. 줄바꿈/들여쓰기 자유 (중괄호가 블록 경계)
- 리스트·딕셔너리 리터럴과 함수 인자에서 **마지막 쉼표를 남겨도 된다** (`[1, 2,]`) — 여러 줄로 쓸 때 편하고, 파이썬도 같다

## 제어문
```
if x >= 90 then { print "A" }        # then 생략 가능
else if x >= 80 { print "B" }
else { print "F" }

while x > 0 do { x -= 1 }            # do 생략 가능

for i = 1 to 10 { print i }          # 양끝 포함
for i = 10 to 1 step -2 { }          # 거꾸로 세려면 step 에 음수를
for i = 1 to len(빈목록) { }        # step 없으면 항상 올라간다 → 빈 리스트면 한 번도 안 돎
                                     # (양끝이 **상수**인데 거꾸로면 에러: for i = 1 to 0)
for x in [1, 2, 3] { }               # 리스트 순회
for ch in "안녕" { }                  # 문자열 글자 순회 (한글 = 1글자)
for k in 딕셔너리 { }                 # 키 순회 (정렬 순서)

break    continue
```

## 함수
```
func 더하기(a, b) { return a + b }    # return 생략 시 0 반환
print 더하기(3, 4)
```
재귀 가능 (깊이 제한 데스크톱 2000 / **웹 플레이그라운드 200** — 브라우저 호출 스택이 훨씬 얕다. 파이썬 기본값은 1000이라 `topython` 이 `sys.setrecursionlimit` 으로 맞춰 준다). 정의보다 위에서 호출 가능 (호이스팅).
함수 안에서 전역 변수 읽기/쓰기 가능, `let` 으로 만들면 지역 변수.

## 리스트 (인덱스는 1부터!)
```
let xs = [10, 20, 30]
print xs[1]                # 10  ← 첫 번째가 1번
xs[2] = 99                 # 수정
xs[2] += 1                 # 원소 복합 대입
let grid = [[1,2],[3,4]]   # 중첩
grid[1][2] = 7
let ys = xs + [40, 50]     # + 로 연결 (새 리스트, 원본 그대로)
print xs == [10, 99, 30]   # == 는 내용 비교 (깊은 비교)
```

`for x in xs` 는 **반복을 시작할 때의 값**을 돈다 — 도는 중에 `push` 하거나 지워도
그 회차는 영향을 받지 않는다. (파이썬의 `for` 는 살아 있는 리스트를 돌아서 무한 루프가
되므로, `topython` 이 필요한 자리에만 사본을 뜬다.)

## 딕셔너리 (키는 문자열만)
```
let d = {"이름": "미르", "나이": 15}
print d["이름"]
d["학교"] = "중학교"        # 새 키 자동 생성
d["나이"] += 1
print d["없는키"]           # 에러! 읽기 전에 has(d, "키") 로 확인
```

## 클래스
```
class 사람 {
    func init(이름) {          # 생성자 (없어도 됨)
        self.이름 = 이름
        self.나이 = 0
    }
    func 인사() { print "안녕, " + self.이름 }
    func 생일() { self.나이 += 1  self.인사() }   # self 로 필드/메서드 접근
}
let p = 사람("미르")           # 클래스 이름이 생성자
p.인사()
p.나이 = 15                   # 필드는 밖에서도 읽기/쓰기/생성 가능
print p                       # 사람{"나이": 15, "이름": "미르"}
```
상속 없음. 딕셔너리에는 `.` 대신 `["키"]` 사용.

## 내장 함수
```
수학:    random(a,b) round(x) round(x,자릿수) floor(x) ceil(x) abs(x) sqrt(x) min(a,b) max(a,b)
변환:    num("15")  str(3)
공통:    len(리스트/문자열/딕셔너리)  reverse(리스트/문자열)
리스트:  push(xs,값) pop(xs) sort(xs) remove(xs,위치)  has(xs,값)  find(xs,값)→위치(없으면 0)
딕셔너리: keys(d) has(d,키) remove(d,키)
문자열:  split(s,구분자) join(xs,구분자) upper(s) lower(s)   # 영문자만 바꾼다
        find(s,찾을것)→위치(없으면 0)  replace(s,바꿀것,새것)  substr(s,시작,개수)
        s[1] 인덱싱 가능 (읽기만)
파일:    readfile(경로) writefile(경로,내용) appendfile(경로,내용) exists(경로)
기타:    time()→초  exit()→즉시 종료  copy(값)→깊은 복사  error("메시지")→에러 발생
```
- `reverse(리스트)` 는 리스트를 **제자리에서** 뒤집는다 (`sort` 와 같음). `reverse("안녕")` 은 뒤집은 **새 문자열**을 준다 — 문자열은 못 바꾸므로.
- 내림차순 정렬은 `sort(xs)` 한 뒤 `reverse(xs)`.
- `round(x, 2)` 는 소수 둘째 자리까지 반올림 (자릿수는 0~15).
- **수를 코드에 적을 때는 `123`·`1.5` 뿐이다** — `1e10`(지수), `0x10`(16진수), `1_000`(밑줄)은 전부 에러이고,
  에러가 무엇을 대신 쓰면 되는지 말해 준다. 지수가 꼭 필요하면 `num("1e10")` 으로는 된다.
- `num(문자열)` 이 받는 것은 **눈에 보이는 그대로의 수**뿐이다 — 앞뒤 공백, 부호, 소수점, `1e3` 같은 지수까지.
  `0x10`·`inf`·`nan`·`1_000` 은 전부 에러다 (C++ 와 파이썬이 이 넷에서 서로 다르게 답해서, 셋 다 받는 것만 남겼다).
  `input` 도 같은 문법으로 본다 — 숫자로 보이면 숫자, 아니면 문자열 그대로.

## 여러 파일로 나누기 (import)
```
import "utils.my"        # 파일 맨 위에, 한 줄에 하나씩. 중복 import 는 자동 무시
```
경로는 **그 import 를 쓴 파일이 있는 폴더 기준**이다 — `venos 프로젝트/main.my` 처럼
바깥에서 불러도 `프로젝트/` 안의 파일을 찾는다. 하위 폴더는 `import "lib/도우미.my"`.
import 는 데스크톱 전용 (웹 플레이그라운드와 REPL 에서는 사용 불가).

## 에러 처리 (try/catch)
```
try {
    let n = num(input "숫자: ")     # 숫자가 아니면 에러
    print 10 / n                    # 0 이면 에러
} catch 오류 {
    print "문제 발생:", 오류        # 오류 변수에 에러 메시지(문자열)가 담김
}
error("직접 만든 에러")             # try 로 잡을 수 있는 에러를 일부러 발생
```
break/continue/return/exit 는 에러가 아니므로 catch 에 잡히지 않고 통과.

에러를 잡지 않으면 실행이 멈추면서 **그 자리까지 오게 된 호출 경로**를 함께 보여 준다 (인터프리터):
```
!! 에러: [줄 2] 인덱스 범위 초과: 10 (리스트 크기: 3, 인덱스는 1부터)
    줄 2 | return xs[10]
    부른 순서: 바깥 (줄 10에서) → 가운데 (줄 8에서) → 안쪽 (줄 5에서)
```

문법을 틀렸을 때는 **줄 안의 자리**까지 `^` 로 가리킨다 (세 실행 방식이 모두 같은 자리를 짚는다 — 같은 파서를 쓴다):
```
!! 에러: [줄 6] if 조건에서 값을 견줄 때는 == 를 씁니다 (= 는 값을 넣을 때)
    줄 6 | if 학생수 = abc { print "같다" }
                     ^
```
실행 도중에 나는 에러(없는 키, 인덱스 범위 등)는 줄까지만 가리킨다.

## 복사 (참조 방식 주의)
```
let b = a           # 리스트/딕셔너리/객체면 같은 것을 가리킴 (b 수정 = a 수정)
let b = copy(a)     # 완전히 독립된 깊은 복사본
```

## 한계 (부딪히면 에러로 말해 준다 — 조용히 죽지 않는다)
| 무엇 | 얼마나 | 넘으면 |
|---|---|---|
| 정수의 정확도 | 약 9천조 (2^53) | 조용히 어긋난다 — 그때는 `topython` (파이썬 정수엔 한계가 없다) |
| 함수 재귀 깊이 | 데스크톱 2000 / 웹 200 | `함수 호출이 너무 깊습니다 (무한 재귀?)` |
| 식·블록 중첩 깊이 | 200단계 | `식이나 블록이 너무 깊게 중첩되었습니다` (괄호를 안 닫으면 여기에 걸린다) |
| 리스트·딕셔너리 중첩 깊이 | 1000단계 | 출력·비교·복사에서 `1000단계보다 깊게 중첩되었거나 자기 자신을 포함한 구조입니다` (둘을 구분할 방법이 없어 문구가 둘 다 말한다) |
| `while` 한 번의 반복 횟수 | 웹만 1000만 회 (데스크톱은 상한 없음) | `반복 횟수가 너무 많습니다 (무한 루프?)` |

웹 플레이그라운드에서는 **도는 도중에 ⏹ Stop(또는 Esc)으로 언제든 멈출 수 있다** —
입력을 기다릴 때든 계산만 돌 때든. 긴 프로그램의 출력도 끝나기를 기다리지 않고 그때그때 보인다.

속도는 CPython 대비 **반복문·리스트 2~3배, 재귀 6배 느리고, 딕셔너리는 비슷하다**
(같은 프로그램을 `topython` 으로 옮겨 비교한 값). 한 대의 컴퓨터에서 잰 값이라
기계와 파이썬 판에 따라 오르내린다 — 재 보고 숫자가 다르다고 뭔가 고장 난 건 아니다.
중요한 건 배율의 자릿수다: **교과서 크기에서는 둘 다 눈 깜짝할 사이다.**

한 가지 예외가 있다: 반복문 안에서
`s = s + 글자` 로 문자열을 늘리면 **Venos 는 길이의 제곱에 비례**한다 (파이썬은 제자리에서
늘려 거의 선형). 2만 자까지는 0.07초라 교과서 크기에서는 보이지 않지만, 16만 자면 5초다.
아주 긴 문자열을 조립할 일이 생기면 그때가 `topython` 을 쓸 자리다.

## 자주 하는 실수 (AI에게 특히 강조)

다른 언어의 버릇(`elif`, `!`, `**`, `//` 주석, `;`, `:` 들여쓰기, `xs[1:3]`, `xs[-1]`,
`"abc".upper()`, `True`/`None`, `f"..."`, `x is y`, `x in xs`, `import math`)으로 쓰면
**무엇을 대신 쓰면 되는지까지 에러가 말해 준다.**

1. **인덱스는 1부터** (0 아님!)
2. `elif` 없음 → `else if`
3. 함수/변수 사용 전 `let` 선언 필수 (함수는 예외: 호이스팅됨)
4. 딕셔너리 키 읽기는 없으면 **에러** — `has()` 로 먼저 확인
5. 문자열 글자 수정 불가 → `replace()` 나 `substr()` 조합
6. `readfile` 전에 `exists()` 확인 (또는 try/catch 로 감싸기)
7. `입력 = input` 처럼 input 뒤에 `=` 쓰지 말 것 → `x = input "질문: "`
8. 논리 부정은 `!` 가 아니라 `not`
9. 내장 함수와 같은 이름으로 함수·클래스를 만들 수 없다 (`func floor(n) { }` 는 에러) — 메서드 이름은 괜찮다
10. `step` 없는 `for` 는 **항상 올라간다** — `for i = 1 to len(A)` 는 빈 리스트에서 한 번도 안 돈다.
    거꾸로 세려면 `step -1` 을 꼭 붙일 것 (양끝이 상수인데 거꾸로면 에러로 알려 준다)
11. `min`/`max` 는 **수만** 받는다 (파이썬과 달리 문자열·리스트는 비교하지 않는다)
12. 딕셔너리 키는 **문자열만** — `딕[1]` 은 에러다
13. 워드·PDF·블로그에서 코드를 붙여넣으면 따옴표가 `“ ”`, 빼기가 `–` 로 바뀌어 있고,
    한글 입력 상태에서 친 공백·괄호는 전각(`　` `（`)이다. 화면에는 멀쩡해 보이므로
    **에러가 그 글자의 이름을 불러 준다** (`U+00A0`, `U+200B` 같은 안 보이는 것도 포함).
    메모장이 파일 앞에 붙이는 BOM 은 에러가 아니라 그냥 건너뛴다
14. **키워드는 영어다 — 한글은 이름에만 쓸 수 있다.** `만약`·`반복`·`함수`·`변수`·`참`
    처럼 한국어로 쓰면 에러가 그 이름을 부르고 무엇을 대신 쓰는지 말해 준다.
    (이름은 `let 학생수 = 30` 처럼 한글로 지어도 된다 — 그래서 생기는 짐작이다)

